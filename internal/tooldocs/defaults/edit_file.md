---
description: |
  Replace exact old_string with new_string in a file (unique match unless replace_all). Any host path. Default blocks; wait_for_output:false for huge files—collect with wait mode=tool.
---

# edit_file

Include enough surrounding context so old_string matches once.
