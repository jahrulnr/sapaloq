---
description: |
  Delete a file (not directories) at any host path. Default blocks; wait_for_output:false rarely needed—collect with wait mode=tool if used.
---

# delete_file

Destructive—confirm path before calling.
