---
description: |
  List entries in a directory (any host path). Default blocks; wait_for_output:false for very large directories—collect with wait mode=tool.
---

# list_dir

Quick directory survey before deeper reads.
