---
description: |
  Load a local image (png/jpeg/gif/webp) into vision so you can SEE it. Requires a vision-capable model. Do NOT use read_file or exec on image bytes. Default blocks; wait_for_output:false for huge images—collect with wait mode=tool.
---

# read_image

Use when the user attaches or references a local image file.
