---
description: |
  Fetch a URL and return readable text (size-capped). Default blocks; wait_for_output:false for slow pages—collect with wait mode=tool.
---

# web_fetch

Follow up web_search or read documentation/API pages.
