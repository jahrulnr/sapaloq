---
description: |
  Find files under a directory using a glob pattern (supports **). Default blocks; wait_for_output:false fires in background for huge trees—collect with wait mode=tool.
---

# glob

Assessment tool for discovering paths before read/edit.
