---
description: |
  Append a timestamped note to the user's personal storage. Destination via storage_id, intent phrase, or mode (personal|work|hobby). Scribe's only write tool. Default blocks; wait_for_output:false rarely needed.
---

# scribe_write_note

Do not use write_file/create_file for scribe capture.
