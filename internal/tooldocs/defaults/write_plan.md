---
description: |
  Persist the planner's Markdown plan (Goal, Constraints, Steps, Risks, Acceptance). Planner-only write to the plan artifact. Default blocks; wait_for_output:false rarely needed.
---

# write_plan

Iterate with read_plan to refine before stopping.
