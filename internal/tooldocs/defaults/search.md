---
description: |
  Search file contents under a directory with a regular expression. Optional filename glob filter. Default blocks; use wait_for_output:false + wait mode=tool for large repos.
---

# search

Use for symbol/string discovery across many files.
